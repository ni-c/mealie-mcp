# Keeps the build context small and reproducible — a stray root-owned 0600 file
# anywhere in the context would otherwise fail the build — and it keeps secrets
# and state away from the daemon.
node_modules
dist
coverage
test
docs
data
.git
.github
.env
*.log
docker-compose*.yml
scripts
