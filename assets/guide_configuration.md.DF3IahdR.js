import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1788283327000}`),a={name:`guide/configuration.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h1><p>See the <a href="/reference/environment">environment variable reference</a> for the full table.</p><h2 id="getting-a-token" tabindex="-1">Getting a token <a class="header-anchor" href="#getting-a-token" aria-label="Permalink to “Getting a token”">​</a></h2><p>Create the token in Mealie under <strong>Settings → API Tokens</strong>. The token acts as the user who created it and inherits that user&#39;s group, household and permission flags — there are no finer-grained scopes in Mealie.</p><p>Give the server a <strong>dedicated, non-admin user</strong>. The admin-only surface is not exposed by any tool here, but a token minted from an admin account still carries those rights if anything else ever reaches the API with it. A sensible user for this server has <code>canOrganize</code> (needed for tags, categories, foods and units) and neither <code>canManage</code> nor <code>canInvite</code>.</p><p>Once read, the token is deleted from the process environment, so child processes cannot pick it up out of <code>/proc/&lt;pid&gt;/environ</code>.</p><h2 id="required-variables" tabindex="-1">Required variables <a class="header-anchor" href="#required-variables" aria-label="Permalink to “Required variables”">​</a></h2><table tabindex="0"><thead><tr><th>Variable</th><th>Description</th></tr></thead><tbody><tr><td><code>MEALIE_URL</code></td><td>Base URL of the instance, e.g. <code>https://mealie.example.com</code></td></tr><tr><td><code>MEALIE_API_TOKEN</code></td><td>Token from Settings → API Tokens; it acts as the user who created it.</td></tr></tbody></table><p>A missing credential is a warning, not a fatal error: the server still completes the MCP handshake and answers <code>tools/list</code>, and every actual call fails with setup instructions. A <strong>malformed</strong> <code>MEALIE_URL</code> exits immediately — that one could send the token to the wrong host. URLs with embedded credentials, a query string or a fragment are rejected for the same reason.</p><h2 id="optional-variables" tabindex="-1">Optional variables <a class="header-anchor" href="#optional-variables" aria-label="Permalink to “Optional variables”">​</a></h2><table tabindex="0"><thead><tr><th>Variable</th><th>Description</th></tr></thead><tbody><tr><td><code>MEALIE_READ_ONLY</code></td><td>Exactly <code>true</code> registers the 18 read tools only</td></tr><tr><td><code>MEALIE_ACCEPT_LANGUAGE</code></td><td>e.g. <code>de-DE</code>; localises unit and label names</td></tr><tr><td><code>MEALIE_INSECURE_TLS</code></td><td>Exactly <code>true</code> accepts a self-signed certificate, scoped to this connection</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">Booleans compare against the literal string <code>true</code></p><p><code>MEALIE_READ_ONLY=1</code>, <code>yes</code> or <code>True</code> all leave the flag <strong>off</strong>. A typo fails off, never on — check the startup line on stderr, which reports the mode in effect.</p></div><h2 id="turning-the-approval-dialog-off" tabindex="-1">Turning the approval dialog off <a class="header-anchor" href="#turning-the-approval-dialog-off" aria-label="Permalink to “Turning the approval dialog off”">​</a></h2><p>The eleven guarded tools ask a person through MCP elicitation before they act. <code>ELICITATION=false</code> takes them to the two-call token instead. It does not remove the guard; there is no setting in which a guarded call goes unannounced.</p><p>The variable deliberately carries no <code>MEALIE_</code> prefix, which means it reaches every MCP server in the same environment, and — unlike the booleans above — a value it does not recognise <strong>stops the server</strong> rather than failing off. See <a href="/guide/approval">Asking a person</a>.</p><h2 id="tls" tabindex="-1">TLS <a class="header-anchor" href="#tls" aria-label="Permalink to “TLS”">​</a></h2><p>Prefer <code>https://</code>. Using plain <code>http://</code> to a non-local host prints a warning: the API token would travel unencrypted.</p><p>If the instance uses a self-signed certificate, <code>MEALIE_INSECURE_TLS=true</code> disables certificate verification <strong>for this connection only</strong> — it does not touch <code>NODE_TLS_REJECT_UNAUTHORIZED</code> or affect any other TLS connection the process makes.</p><h2 id="localisation" tabindex="-1">Localisation <a class="header-anchor" href="#localisation" aria-label="Permalink to “Localisation”">​</a></h2><p><code>MEALIE_ACCEPT_LANGUAGE</code> (e.g. <code>de-DE</code>) is sent as the <code>accept-language</code> header, which Mealie honours on nearly every endpoint. It localises unit names, label names and validation messages. Unset, the choice is left to the server default.</p><h2 id="choosing-the-tools-that-load" tabindex="-1">Choosing the tools that load <a class="header-anchor" href="#choosing-the-tools-that-load" aria-label="Permalink to “Choosing the tools that load”">​</a></h2><p>Read-only mode is one cut, along a line this server drew for you. <code>MEALIE_ALLOW_TOOLS</code> and <code>MEALIE_DENY_TOOLS</code> let you draw your own:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">MEALIE_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">essential</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">MEALIE_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">search_recipes,get_recipe,import_recipe_from_url</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">MEALIE_DENY_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">delete_*</span></span></code></pre></div><p>Why bother, when all fifty-two work: a model chooses the right tool far more reliably from a handful than from a long list, and every tool it can see costs context on every single request. If this is the only MCP server in a session, fifty-two is fine. If it is one of six, it is not.</p><p><strong>The syntax.</strong> Comma-separated entries. An entry is either an exact tool name or a prefix with a trailing <code>*</code> — <code>list_*</code> matches every tool whose name starts with <code>list_</code>. Entries are trimmed and case-insensitive, empty ones are ignored, and an empty value counts as unset. Nothing else is a pattern: <code>*_x</code> and <code>list_*_x</code> are rejected rather than silently matching nothing.</p><p><strong><code>essential</code></strong> is a curated preset of eight:</p><p><code>search_recipes</code>, <code>get_recipe</code>, <code>import_recipe_from_url</code>, <code>create_recipe</code>, <code>get_todays_meals</code>, <code>create_mealplan_entry</code>, <code>list_shopping_lists</code>, <code>add_recipe_to_shopping_list</code>.</p><p>It composes — naming a tool alongside it puts that one back, and <code>MEALIE_DENY_TOOLS</code> takes one away.</p><p><strong>Both together.</strong> <code>MEALIE_ALLOW_TOOLS</code> decides what is in; <code>MEALIE_DENY_TOOLS</code> is then subtracted from the result. With only a deny list, everything else stays.</p><p><strong>A name that matches nothing stops the server</strong>, with the offending entry and the list of real names. That is deliberate: the alternative is a tool quietly missing from <code>tools/list</code>, and nobody traces an absence back to an environment variable. The same applies to a pattern that matches no tool.</p><p><strong>With read-only mode</strong>, the write tools are not registered at all, so naming one explicitly in <code>MEALIE_ALLOW_TOOLS</code> is an error that says so — rather than calling a tool unknown when it plainly exists. A <em>pattern</em> that covers write tools is fine and simply contributes nothing, and <code>MEALIE_ALLOW_TOOLS=essential</code> narrows to the read half of the preset.</p><div class="tip custom-block"><p class="custom-block-title">It is the same cut, not a second one</p><p>A filtered tool is never registered, so it is absent from <code>tools/list</code> and unknown to <code>tools/call</code> alike — exactly what <code>MEALIE_READ_ONLY</code> does to a write tool. There is no &quot;hidden but callable&quot; state to reason about.</p></div>`,32)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};