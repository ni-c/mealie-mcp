import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1787084764000}`),a={name:`reference/changelog.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="_0-3-0-2026-09-03" tabindex="-1"><a href="https://github.com/ni-c/mealie-mcp/releases/tag/v0.3.0" target="_blank" rel="noreferrer">0.3.0</a> - 2026-09-03 <a class="header-anchor" href="#_0-3-0-2026-09-03" aria-label="Permalink to “0.3.0 - 2026-09-03”">​</a></h2><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li><p>Every tool declares an <code>outputSchema</code> and answers with <code>structuredContent</code> beside the text block. A client no longer has to parse prose to use a result — which nine of them made unavoidable, since they answered with a sentence. The sentence stays, in the text block.</p><p>Most tools carry <code>untrusted: true</code> and <code>source: &quot;mealie&quot;</code> as fields, not only as a preamble in the text: recipes are routinely scraped from arbitrary websites and comments come from other users of the instance. The ten without the marker answer with an id this server was given, or — for <code>get_about</code> — a version string and the permission flags of the account it authenticates as.</p><p>Mealie&#39;s records are described as open objects with the top-level keys this server builds. A self-hosted Mealie is any release, and a strict shape would turn a field one adds into a tool that fails outright.</p></li><li><p>Tools that need a confirmation now <strong>ask the user</strong>, on clients that can show a prompt. The two-call <code>confirm_token</code> remains for clients that cannot, so nothing that works today stops working — but where a person can be asked, one is, instead of a token that only proves the same call was made twice. This covers all guarded tools, <code>create_share_token</code> among them.</p></li><li><p><strong><code>delete_share_token</code> now asks too.</strong> Its description said in so many words that it needed &quot;no confirmation — this narrows access rather than widening it&quot;, and the direction really is the safe one. What is not is that the link cannot be reissued: a new share token is a different URL, so whoever was sent the old one finds a dead link, and this server cannot tell whom that was.</p></li><li><p><code>ELICITATION</code> switches the dialog off — <code>false</code> sends a client that could have been asked down the two-call-token path instead. For a scheduled job or a test harness, where a dialog is the wrong shape rather than an unwanted one.</p><p>It does <strong>not</strong> remove the guard: there is no setting in which a guarded call goes unannounced. Two deliberate rough edges come with it. The variable is <strong>not prefixed</strong>, so one <code>export ELICITATION=false</code> reaches every MCP server in the environment — which is why a server started with it off prints a line saying so, and why the fallback text names the server instead of blaming a client that was working fine. And a value that is neither <code>true</code> nor <code>false</code><strong>stops the server</strong>, where the <code>MEALIE_*</code> booleans beside it fail <em>off</em> on a typo: this is the only variable here that defaults to <em>on</em>. It is read after <code>MEALIE_API_TOKEN</code> is wiped from the environment, so that exit cannot leave the token behind.</p></li><li><p>A <code>docs/guide/approval.md</code> page.</p></li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li><p>The advertised schemas avoid spellings that are legal JSON Schema and still get a tool refused, or its constraint silently dropped, by some MCP clients: an open object now writes <code>&quot;additionalProperties&quot;: true</code> rather than the empty schema <code>{}</code> zod emits for it; and a nullable field is written as <code>anyOf</code> branches rather than <code>&quot;type&quot;: [&quot;string&quot;, &quot;null&quot;]</code>, which several clients read as a single type and then drop. What the tools accept and return is unchanged; only the way the schema says so is.</p></li><li><p>A result too large to shrink is now an error rather than an envelope carrying the oversized document as a string. That envelope is valid JSON and no longer a valid <em>answer</em>: the SDK checks a result against the schema its tool declares.</p></li><li><p><code>update_recipe</code> with no fields answers <code>{recipe, changed: false, note}</code> rather than the bare sentence. It is still not an error — a model that resolved every field to its current value should not be punished for asking.</p></li><li><p>The two-call <code>confirm_token</code> prompt is an error result. What was asked for did not happen, which is what <code>isError</code> says. The text is unchanged and still carries the token.</p></li><li><p><code>MEALIE_READ_ONLY</code> now accepts <code>1</code>, <code>true</code> and <code>yes</code> in any case and ignores surrounding whitespace, matching the rest of the family. It only ever takes capability away, so an operator who wrote <code>MEALIE_READ_ONLY=True</code> meant the safe thing and now gets it — where before that spelling silently left every write tool registered. <code>MEALIE_INSECURE_TLS</code> stays exactly <code>true</code> on purpose: it weakens the server, so only the one unambiguous spelling should do it.</p></li><li><p>The shared libraries move to <code>mcp-approval</code> 0.7.1, <code>mcp-tool-allowlist</code> 0.2.1, <code>mcp-internal-hosts</code> 0.2.1, <code>mcp-integration-harness</code> 0.2.0 and <code>svg-asset-set</code> 0.2.0.</p></li><li><p>Runs on <strong>MCP SDK 2.0</strong>. Existing clients see the same protocol revision they always did; the change is the package layout behind it, and it is what lets the dialog above work on both protocol eras from one code path — including behind a stateless gateway, where the older mechanism silently fell back to the weaker token for every client.</p></li><li><p>The linter is <strong>oxlint</strong> instead of eslint plus typescript-eslint, which lifts the TypeScript ceiling: typescript-eslint pins <code>typescript</code> below 6.1, so this repository was held on TypeScript 6 by its linter rather than by its code.</p></li><li><p>The tool filter, the confirmation store, the host classifier and the documentation-asset generator now come from <strong><code>mcp-tool-allowlist</code></strong>, <strong><code>mcp-approval</code></strong>, <strong><code>mcp-internal-hosts</code></strong> and <strong><code>svg-asset-set</code></strong> rather than from copies kept here — 802 fewer lines, and one place to fix each. None of them has a runtime dependency of its own.</p></li><li><p>A <code>confirm_token</code> that does not match its arguments is <strong>refused with the reason</strong> instead of being answered with a fresh prompt, in the same words as every other server in the family. The binding is unchanged: a confirmation issued for one recipe still cannot delete another. A wrong token also no longer voids the outstanding one — voiding it let anyone who could reach the tool cancel a pending confirmation by sending rubbish, which protected nothing.</p></li><li><p>stdio is served through <code>serveStdio</code>, so the connection&#39;s era is negotiated on the opening exchange rather than assumed. A client that pins the <code>2026-07-28</code> era is served it; until now its <code>server/discover</code> probe was answered with &quot;Method not found&quot; and only <code>2025-11-25</code> was on offer. A client that speaks the older era sees no change — it is still pinned to one instance for the life of the connection, exactly as a hand-wired <code>StdioServerTransport</code> served it.</p></li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p><strong><code>search_recipes</code> answered a narrowed question with the whole collection.</strong> The tool described <code>tags</code>, <code>categories</code> and <code>tools</code> as taking &quot;names, slugs or UUIDs&quot;. Mealie takes no names: <code>_uuids_for_items</code> looks a non-UUID up as a slug, returns an empty list when nothing matches, and <code>_build_recipe_filter</code> then tests <code>if tags:</code> — so the filter is not attached at all. Measured on v3.22.0 with three recipes, one tagged &quot;Weeknight Dinner&quot;: <code>?tags=Weeknight%20Dinner</code> returned all three, as did the mistyped slug <code>weeknight-dinnerrr</code>, with nothing in the answer saying it had not been filtered. Names and slugs are now resolved to ids before the search runs, and an entry that resolves to nothing is an error.</p></li><li><p><strong><code>search_recipes</code> promised AND and did not deliver it.</strong> <code>cookbook</code> and the organizer filters are mutually exclusive in Mealie — <code>_build_recipe_filter</code> returns the cookbook&#39;s own filter and returns early — so <code>{cookbook: &quot;desserts&quot;, tags: [&quot;vegan&quot;]}</code> silently ignored the tags. Confirmed live. The combination is now refused.</p></li><li><p><strong><code>search_recipes({foods: […]})</code> could only 500.</strong> Mealie resolves foods not at all and puts the value straight into <code>RecipeIngredientModel.food_id == food</code>, so a name or slug reaches the <code>GUID</code> type decorator and comes back as HTTP 500. <code>foods</code> now takes UUIDs only, as <code>suggest_recipes</code> always has.</p></li><li><p><strong><code>order_by: &quot;random&quot;</code> always failed.</strong> Mealie&#39;s pagination model validates <code>paginationSeed is required when orderBy is random</code> and answers HTTP 422; the tool took no seed, so the option could not be used. It now generates one per call.</p></li><li><p>Mealie&#39;s organizer slug routes answer &quot;no such slug&quot; two different ways — <code>/categories/slug/nope</code> is a clean 404 while <code>/tags/slug/nope</code> and <code>/tools/slug/nope</code> are HTTP 500. The lookup reads both as a miss and falls through to the name search; a Mealie that is genuinely failing still reports the failure, because that second request has to succeed for anything to resolve. Found by the integration suite, not by reading.</p></li><li><p><strong><code>merge_foods</code> and <code>merge_units</code> named the wrong tool.</strong> Both come out of one factory, and the factory passed <code>toolName: &#39;create_unit&#39;</code> — a real, unrelated tool of this server. That name is printed in two places a caller acts on: the fallback instruction (&quot;call <code>create_unit</code> again with the token&quot;) and the sentence after a decline. Callers were being pointed at something that creates rather than merges. Introduced on 2026-09-01 with the move to <code>mcp-approval</code>.</p></li><li><p>Four <code>update_*</code> tools were annotated <code>destructiveHint: false</code>: <code>update_recipe</code>, <code>update_organizer</code>, <code>update_mealplan_entry</code> and <code>update_shopping_list_items</code>. Mealie keeps no version history, so replacing an instruction list leaves nowhere to read the old one back from — that is the definition the whole family uses, and Wiki.js&#39;s <code>update_page</code> is genuinely on the other side of it because Wiki.js <em>has</em> page history. The difference is the backend, not the verb.</p></li><li><p>Confirmation tokens are compared with a <strong>constant-time</strong> comparison. The copy in this repository used <code>!==</code>, which leaks through timing how much of a guess was right. Reaching a token still requires having received it in a previous tool result, so this closes a margin rather than a hole.</p></li><li><p>An entry in <code>MEALIE_ALLOW_TOOLS</code> that is not tool-name-shaped is now <strong>redacted</strong> in the error rather than quoted back. <code>MEALIE_TOKEN</code> and <code>MEALIE_ALLOW_TOOLS</code> are adjacent lines in every compose file, and a paste into the wrong one used to print the credential into the client&#39;s log.</p></li></ul><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h3><ul><li><p><strong>The confirmation gate was drawn along the wrong line.</strong> It followed the tool <em>name</em> — everything called <code>delete_*</code> or <code>merge_*</code> asked — rather than &quot;cannot be undone&quot;, and four tools fell in the gap. <code>update_recipe</code> with <code>{ingredients: [], instructions: []}</code> emptied a recipe in one call and answered with the now-empty recipe, where <code>delete_recipe</code> on the same recipe cost two calls and a token; Mealie keeps no version history, so both are equally final. <code>update_organizer</code>, <code>update_mealplan_entry</code> and <code>update_shopping_list_items</code> were the other three.</p><p>The line is now the one <code>annotations.ts</code> always stated — content a person wrote, replaced with no way back — applied per <em>call</em> rather than per tool. <code>update_recipe</code> asks when it replaces name, description, ingredients, instructions, tags, categories or notes, and goes straight through for times, servings, yield and the source link. <code>update_shopping_list_items</code> asks for <code>note</code> and not for ticking off. <code>update_mealplan_entry</code> asks for <code>title</code> and <code>text</code> and not for a move. <code>update_organizer</code> always asks: a rename regenerates the slug. Each approval is bound to a fingerprint of the replacing values as well as to the target, so one shown for one new instruction list cannot be spent on a call that clears the list instead.</p><p>The lasting part is <code>test/gating.test.ts</code>, which claims this over the whole catalogue: every tool annotated <code>destructiveHint: true</code> has to accept a <code>confirm_token</code>, has to write nothing on its first call, and has to appear in that file&#39;s table. A per-tool test would not have found the gap — every per-tool test that existed passed.</p></li><li><p><strong><code>create_cookbook(is_public: true)</code> published without asking.</strong> The one other tool that widens who can see something; <code>create_share_token</code> has been guarded since it existed. It exposes less than a share link — the recipes themselves need <code>settings.public</code> of their own — but the name, description and saved filter go out, and there is no <code>update_cookbook</code> to take it back with. A private cookbook is still created without a prompt.</p></li><li><p><strong><code>import_recipe_from_html_or_json</code> claimed <code>openWorldHint: false</code></strong> while Mealie fetched an address out of the document it was handed. Verified against v3.22.0: a pasted <code>{&quot;image&quot;: &quot;http://…/latest/meta-data/&quot;}</code> puts <code>Image URL: …</code> in Mealie&#39;s log and goes through <code>recipe_data_service.scrape_image</code>. Mealie&#39;s own guard refuses on <code>is_private</code>, which is False for <code>100.100.100.200</code> (Alibaba Cloud metadata) and for all of <code>100.64.0.0/10</code>. The tool now carries <code>openWorldHint: true</code>, and the addresses this server can find in the document — schema.org <code>image</code>/<code>thumbnailUrl</code>/<code>contentUrl</code> in all three shapes, <code>&lt;img src&gt;</code>, <code>og:image</code> — go through the same <code>assertFetchableUrl</code> as a URL argument.</p></li><li><p><strong><code>source_url</code> was the only URL argument with no scheme check.</strong> <code>httpUrl</code> exists because zod&#39;s <code>.url()</code> accepts <code>javascript:</code>, <code>file:</code> and <code>data:</code>; <code>source_url</code> was a plain string, Mealie does not validate <code>org_url</code> either, and the value comes back to every reader through <code>recipeDetail</code>. It is now <code>httpUrl</code>.</p></li></ul><h2 id="_0-2-0-2026-08-27" tabindex="-1">[0.2.0] - 2026-08-27 <a class="header-anchor" href="#_0-2-0-2026-08-27" aria-label="Permalink to “[0.2.0] - 2026-08-27”">​</a></h2><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><ul><li><p><code>MEALIE_ALLOW_TOOLS</code> and <code>MEALIE_DENY_TOOLS</code> choose which of the 52 tools are registered. Both take comma-separated tool names or a prefix with a trailing <code>*</code>, the allow list decides what is in and the deny list is subtracted from it, and <code>MEALIE_ALLOW_TOOLS=essential</code> selects a curated eight — <code>search_recipes</code>, <code>get_recipe</code>, <code>import_recipe_from_url</code>, <code>create_recipe</code>, <code>get_todays_meals</code>, <code>create_mealplan_entry</code>, <code>list_shopping_lists</code>, <code>add_recipe_to_shopping_list</code>. A model picks the right tool far more reliably from eight than from fifty-two, and every visible tool costs context on every request. Nothing changes for an installation that sets neither.</p><p>A filtered tool is not registered at all, so it is absent from <code>tools/list</code> and answers <code>tools/call</code> with &quot;tool not found&quot; — the same cut <code>MEALIE_READ_ONLY</code> already makes, not a second, weaker one.</p><p>An entry that matches no tool <strong>stops the server at startup</strong>, naming the entry and listing the real names, rather than being ignored: an ignored typo leaves a tool missing from <code>tools/list</code> with nothing pointing at the cause.</p></li></ul><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The README now carries the same eight badges, in the same order, as every other MCP server in this family, all of them reading from npm rather than hard-coded; the opening follows one shape; and the standalone &quot;Full documentation&quot; line is gone, because the docs badge three lines above it points at the same page.</li></ul><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The container image no longer ships OpenSSL 3.5.7-r0, which carries <strong>CVE-2026-14456</strong> (denial of service via unbounded memory growth). The pinned <code>node:24-alpine</code> digest is already the newest one; Alpine&#39;s fixed 3.5.8-r0 has simply not been rebuilt into it yet, so the runtime stage now upgrades <code>libcrypto3</code> and <code>libssl3</code> by name. Upgrading those two rather than running a blanket <code>apk upgrade</code> keeps the rest of the image exactly as the digest pins it. The step can go once the base image ships the fix.</li></ul><h2 id="_0-1-2-2026-08-26" tabindex="-1"><a href="https://github.com/ni-c/mealie-mcp/releases/tag/v0.1.2" target="_blank" rel="noreferrer">0.1.2</a> - 2026-08-26 <a class="header-anchor" href="#_0-1-2-2026-08-26" aria-label="Permalink to “0.1.2 - 2026-08-26”">​</a></h2><h3 id="security-1" tabindex="-1">Security <a class="header-anchor" href="#security-1" aria-label="Permalink to “Security”">​</a></h3><ul><li>The host of an import URL is now classified numerically instead of by comparing strings, and a hostname is resolved before it is accepted. <code>http://localhost./recipe</code> — the same name with its root label — walked past the old <code>host === &#39;localhost&#39;</code> and <code>endsWith(&#39;.local&#39;)</code> checks, as did <code>http://nas.local./</code>. A DNS name pointing at <code>127.0.0.1</code> walked past all of it, because a Zod refinement is synchronous and cannot resolve anything. The check therefore moved out of the schema and into the tool handlers, where it can.</li><li>What is sent to Mealie is the parsed URL rather than the string that came in, so the address that was checked is the one Mealie fetches. <code>http://ok.example.com\\@127.0.0.1/recipe</code> has the host <code>ok.example.com</code> for a URL parser and <code>127.0.0.1</code> for a fetcher that splits at the <code>@</code>.</li><li>The hostnames of the cloud metadata service — <code>metadata.google.internal</code>, <code>instance-data</code> and their siblings — are refused by name. They resolve to <code>169.254.169.254</code> on the instance and to nothing anywhere else, so resolving them is exactly what cannot catch them. So are the endpoints that sit outside <code>169.254/16</code>: <code>100.100.100.200</code> (Alibaba Cloud) and <code>192.0.0.192</code> (Oracle).</li><li>An IPv6 scope id is stripped before the address is read. <code>net.isIP</code> accepts <code>::ffff:127.0.0.1%eth0</code>, which made the dotted-quad fold miss its anchor and the address come out as routable. A URL cannot carry one, but a resolver answer can.</li></ul><h3 id="changed-2" tabindex="-1">Changed <a class="header-anchor" href="#changed-2" aria-label="Permalink to “Changed”">​</a></h3><ul><li><strong>Private addresses are no longer refused here.</strong> Loopback, link-local and the metadata endpoints are still refused, but <code>10/8</code>, <code>172.16/12</code>, <code>192.168/16</code>, <code>fc00::/7</code>, the rest of carrier-grade NAT and the <code>.lan</code>/<code>.local</code>/<code>.internal</code>/<code>.home</code> suffixes are not. That is a consistency change, not a new capability: Mealie has refused private addresses in its own HTTP transport since v1.4.0, so an import that names one still fails — further downstream and with a less helpful message. What this server now guards is the set of addresses Mealie does <em>not</em> guard for itself, which is why the two metadata endpoints outside <code>169.254/16</code> were added. If you were relying on this check to keep Mealie off your LAN, that job belongs to Mealie&#39;s own egress rules; <code>SECURITY.md</code> says what is and is not covered.</li><li>Bracketed IPv6 literals are no longer refused as a class. They were, because classifying them piecemeal was thought to be a losing game — the IPv4-mapped forms in particular. The classifier now unwraps those forms instead, so a public IPv6 address works while <code>[::ffff:169.254.169.254]</code> is still caught.</li><li>A name that a resolver sinkholes — answering <code>0.0.0.0</code> or <code>::</code>, which is what every ad blocker and DNS filter does — is no longer refused as a loopback address. That is the resolver declining to answer rather than the name addressing the machine, and describing it as loopback made every blocklisted domain unusable with a message that was simply wrong.</li><li>The description of <code>import_recipe_from_html_or_json</code> no longer claims the tool works &quot;without Mealie fetching anything&quot;. Mealie does not fetch the page, but it does read the image address out of the supplied document and retrieve that — an address this server never sees.</li></ul><h2 id="_0-1-1-2026-08-18" tabindex="-1"><a href="https://github.com/ni-c/mealie-mcp/releases/tag/v0.1.1" target="_blank" rel="noreferrer">0.1.1</a> - 2026-08-18 <a class="header-anchor" href="#_0-1-1-2026-08-18" aria-label="Permalink to “0.1.1 - 2026-08-18”">​</a></h2><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>First release published through the automated pipeline, with npm provenance and the MCP registry entry. 0.1.0 had been published manually while setting up Trusted Publishing and therefore carries no provenance attestation.</li></ul><h2 id="_0-1-0-2026-08-18" tabindex="-1"><a href="https://github.com/ni-c/mealie-mcp/releases/tag/v0.1.0" target="_blank" rel="noreferrer">0.1.0</a> - 2026-08-18 <a class="header-anchor" href="#_0-1-0-2026-08-18" aria-label="Permalink to “0.1.0 - 2026-08-18”">​</a></h2><h3 id="added-2" tabindex="-1">Added <a class="header-anchor" href="#added-2" aria-label="Permalink to “Added”">​</a></h3><ul><li>Initial implementation: MCP server for <a href="https://mealie.io" target="_blank" rel="noreferrer">Mealie</a>, the self-hosted recipe manager and meal planner.</li><li>52 tools — 17 read tools that are always registered, 35 write and import tools that are omitted when <code>MEALIE_READ_ONLY=true</code>: recipe search and detail, recipe CRUD, import from URL / HTML / schema.org JSON / photo, tags, categories and tools, foods, units and the ingredient parser, meal plans, shopping lists including recipe-to-list, cookbooks, ratings, comments, timeline entries and public share links.</li><li>Confirmation tokens on the nine operations that delete, merge or widen access. Tokens are single-use, bound to the target, and for set operations bound to a fingerprint of the whole sorted id set.</li><li>URL validation that restricts the import tools to public <code>http</code>/<code>https</code> addresses, because Mealie performs those fetches from inside its own network.</li><li>All instance content is returned behind an explicit untrusted-content marker.</li></ul>`,28)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};